{
  "name": "@ttpos/share-tsconfig",
  "version": "1.0.0",
  "description": "tsconfig for ttpos share",
  "private": true,
  "scripts": {
    "prepare": "ts-patch install -s"
  },
  "devDependencies": {
    "ts-patch": "^3.3.0"
  }
}
